Simple login system. Members' information is encrypted with bcrypt and then stored in MySQL.

The tokens are issued as JWT tokens.

Also implements a Login Verification System using codes and tokens.
